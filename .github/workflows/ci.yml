name: CI
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  backend:
    runs-on: ubuntu-latest
    defaults: {run: {working-directory: backend}}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with: {python-version: '3.11', cache: pip, cache-dependency-path: backend/requirements-dev.lock}
      - run: pip install -r requirements-dev.lock
      - run: ruff format --check . && ruff check .
      - run: mypy app
      - run: pytest --cov=app --cov-report=term-missing
      - run: pip-audit -r requirements.lock
  frontend:
    runs-on: ubuntu-latest
    defaults: {run: {working-directory: frontend}}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22, cache: npm, cache-dependency-path: frontend/package-lock.json}
      - run: npm ci
      - run: npm run lint && npm run typecheck && npm test && npm run build
      - run: npm audit --audit-level=high
  generator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with: {python-version: '3.11'}
      - uses: actions/setup-node@v4
        with: {node-version: 22}
      - run: pip install pytest -r requirements-design.txt
      - run: pytest tests/test_scaffold.py tests/test_design_system.py tests/test_common_look_and_feel.py -q
      - name: Validate DESIGN.md and generated token formats
        run: |
          npx -y @google/design.md@0.4.0 lint DESIGN.md
          npx -y html-validate@10.4.0 .agents/skills/common-look-and-feel/templates/admin-cms.html
          python3 scripts/export_design_system.py --check
          curl -fsSL https://www.designtokens.org/schemas/2025.10/format.json -o /tmp/dtcg-format-schema.json
          npx -y ajv-cli@5 validate --spec=draft7 --strict=false --all-errors \
            -s /tmp/dtcg-format-schema.json -d design-system/tokens.json
  containers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: frontend/.node-version
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
        working-directory: frontend
      - run: npx playwright install --with-deps chromium
        working-directory: frontend
      - run: docker compose config --quiet
      - run: docker compose up -d --build
      - name: Wait for production entrypoint
        run: |
          curl --fail --retry 20 --retry-delay 2 --retry-all-errors \
            http://127.0.0.1:8080/api/v1/health/ready
          test "$(docker compose ps --status running --services | wc -l)" -eq 2
          docker compose ps --status running
      - name: Verify production security headers
        run: |
          headers="$(curl --fail --silent --show-error --head http://127.0.0.1:8080/)"
          grep -iq '^X-Content-Type-Options: nosniff' <<<"$headers"
          grep -iq '^Referrer-Policy: strict-origin-when-cross-origin' <<<"$headers"
          grep -iq '^Permissions-Policy:' <<<"$headers"
      - run: npm run e2e
        working-directory: frontend
        env:
          E2E_BASE_URL: http://127.0.0.1:8080
      - if: always()
        run: docker compose logs --no-color
      - if: always()
        run: docker compose down -v
